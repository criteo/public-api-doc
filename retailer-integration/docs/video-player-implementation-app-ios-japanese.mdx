---
title: ビデオプレーヤーの実装（アプリ：iOS）
original_file: to-migrate/criteo-dev-portal-full-export/merged_export/Delivery_Docs/v2026_01/docs/Commerce video (japanese)/video-player-implementation-app-ios-japanese.md
description: This is a translated version of the Commerce Onsite Video documentation
hidden: true
---

import Commercevideobeta from "/snippets/retailer-integration/Commercevideobeta.mdx";

<Commercevideobeta />

***

# はじめに

このドキュメントの目的は、**iOS モバイルプラットフォームでの On-Site Commerce Video 広告の実装**に関する詳細なガイドラインを提供することです。

<Info>
  **Android 対 iOS**

  **このページでは、iOS 向けビデオ プレーヤーの実装についてのみ説明します**。 Android での実装については、[こちらのページ](/retailer-integration/docs/video-player-implementation-app-android-japanese)を参照してください。
</Info>

<Info>
  **アプリとウェブ（デスクトップとモバイル）**

  このページでは、**モバイルアプリでの実装**にフォーカスしています。 対象を広げるには、[ウェブ向けビデオプレーヤー実装ガイド](/retailer-integration/docs/video-player-implementation-guide-desktopmobile-japanese)を参照してください。
</Info>

<Warning>
  **バージョン要件**

  iOS の場合、バージョン**9.0**以降をサポートしていますが、最新の5つのバージョンをサポートすることを選択できます。
</Warning>

***

# ビデオプレーヤー実装ガイド

動画広告を実装する際には、以下の大まかな手順を考慮します。

1. VAST tag を要求して解析する
2. 広告を表示するビデオプレーヤーを作成する
3. 広告セッションを設定する
4. 動画再生中に適切なタイミングでビーコンを起動する
5. 広告セッションを終了する
6. クローズドキャプションを処理する

<Info>
  Criteo は **VAST 4.2** をサポートしています（IAB 仕様は[こちら](https://iabtechlab.com/wp-content/uploads/2019/06/VAST_4.2_final_june26.pdf)で入手できます）。
</Info>

***

## ステップ1：VAST タグのリクエストと解析

ビデオプレーヤーのデータを取得するには、まず Criteo API の広告レスポンスから VAST 情報を取得します。

以下は、VAST ファイル（必要な場合）をリクエストし、XPath を解析に使用して、以下を取得する方法の例です。

* 動画メディアファイルのリンク
* VAST XMLトラッキングビーコン
* OMID 検証 URL と `VerificationParameters`
* クローズド・キャプション・ファイル

```swift expandable
public func fetchVAST(_ remoteURL: URL,
                          _ statusHandler: @escaping (_ message: String) -> (),
                          _ completionHandler: @escaping (_ videoAdURL: URL, 
                                                          _ captionsURL: URL,
                                                          _ verificationURL: String, 
                                                          _ verificationParameters: String, 
                                                          _ vendorKey: String, 
                                                          _ tracking: Dictionary&lt;String, URL&gt;) -> (),
                          _ errorHandler: @escaping (_ message: String) -> ()) {
    DispatchQueue.main.async {
        statusHandler("Fetching VAST...");
    }

    print("Fetching VAST from '\(remoteURL)'.")
    self.VASTDownloadTask = URLSession.shared.downloadTask(with: remoteURL) {
        (fileURL, response, error) in
        guard let fileURL = fileURL else {
            DispatchQueue.main.async {
                errorHandler("Unable to fetch VAST from remote URL: \(remoteURL)")
            }
            return
        }
        
        do {
            let fileData = try Data(contentsOf: fileURL)
// 必要に応じてファイルを処理します
            let mediaFile = self.evaluateXPath(xmlString: fileData, xpath: "//MediaFile/text()");
            let captions = self.evaluateXPath(xmlString: fileData, xpath: "//ClosedCaptionFile/text()");
            let verifScript = self.evaluateXPath(xmlString: fileData, 
                                                 xpath: "//Verification[@vendor='criteo.com-omid']/JavaScriptResource/text()");
            let verifParameters = self.evaluateXPath(xmlString: fileData, 
                                                     xpath: "//Verification[@vendor='criteo.com-omid']/VerificationParameters/text()");
// VAST トラッキング URL を取得します
            var tracking: Dictionary&lt;String, URL&gt; = Dictionary(minimumCapacity: 15);
            let events = self.evaluateXPath(xmlString: fileData, xpath: "//Linear/TrackingEvents/Tracking/@event");
            events.forEach {
                let url = self.evaluateXPath(xmlString: fileData, xpath: "//Linear/TrackingEvents/Tracking[@event='"+$0+"']");
                tracking[$0] = URL(string: url[0]);
            }
            tracking["impression"] = URL( string: self.evaluateXPath(xmlString: fileData, xpath: "//Impression")[0]);
            tracking["error"] = URL(string: self.evaluateXPath(xmlString: fileData, xpath: "//Error")[0]);
            tracking["click"] = URL(string: self.evaluateXPath(xmlString: fileData, xpath: "//VideoClicks/ClickTracking/text()")[0]);
// URL を作成します
            let videoAdURL = URL(string: mediaFile[0])!;
            let captionsURL = URL(string: captions[0])!;
            let verificationURL = verifScript[0];            
            DispatchQueue.main.async {
                completionHandler(videoAdURL, captionsURL, verificationURL, verifParameters[0], "criteo", tracking)
            }
        } catch {
            DispatchQueue.main.async {
                errorHandler("Unable to read file content: \(error.localizedDescription)")
            }
        }
    }

    self.VASTDownloadTask?.resume()
}
```

および以下の XPath を評価する解析機能：

```swift expandable
private func evaluateXPath(xmlString: Data, xpath: String) -> [String] {
  var results: [String] = [];
// XML 文字列を C 文字列に変換します
  let bytes = UnsafeMutablePointer&lt;UInt8&gt;.allocate(capacity: xmlString.count + 1)
  defer { bytes.deallocate() }
  xmlString.copyBytes(to: bytes, count: xmlString.count)
  bytes[xmlString.count] = 0;
  let xmlCString = UnsafeRawPointer(bytes).assumingMemoryBound(to: CChar.self)
// XML を解析します
  let xmlDoc = xmlParseDoc(xmlCString)
  guard let doc = xmlDoc else { return results }
// XPath コンテキストを作成します
  let xpathCtx = xmlXPathNewContext(doc)
  guard let context = xpathCtx else {
    xmlFreeDoc(doc)
    return results
  }
// XPath 式を評価します
  let xpathObj = xmlXPathEvalExpression(xpath, context)
  guard let xpathObject = xpathObj else {
    xmlXPathFreeContext(context)
    xmlFreeDoc(doc)
    return results
  }
// ノードを抽出します
  let nodes = xpathObject.pointee.nodesetval
  if let nodeSet = nodes {
    for i in 0..<nodeSet.pointee.nodeNr {
      if let node = nodeSet.pointee.nodeTab[Int(i)] {
        if let content = xmlNodeGetContent(node) {
          let cString = content.withMemoryRebound(to: CChar.self, capacity: 1) { $0 }
          if let result = String(cString: cString, encoding: .utf8) {
            results.append(result)
          }
          xmlFree(content)
        }
      }
    }
  }
// クリーンアップを実行します
  xmlXPathFreeObject(xpathObject)
  xmlXPathFreeContext(context)
  xmlFreeDoc(doc)

  return results
}
```

***

## ステップ2：ビデオプレーヤーの作成

<Info>
  **UIKit と SwiftUI**

  iOS のビデオプレーヤーの例では、従来通り、インターフェイスビルダーとビューコントローラーを使用する `UIKit` を使用してインターフェイスを構築しています。 `SwiftUI` を使用している場合は、[こちらの Apple ドキュメント](https://developer.apple.com/tutorials/swiftui/interfacing-with-uikit)を参照して、`UIKit` ビューとビューコントローラーを `SwiftUI` ビュー内に統合してください。
</Info>

当社のビデオプレーヤーは、ネイティブ コンポーネント `AVPlayer` を使用して構築されており、ビデオプレーヤーの操作に役立つ `play()`、`pause()` などのメソッドや、`volume`、`
duration` などのプロパティを提供します。 また、ビデオプレーヤーの仕様に従うため、クリックリスナーを使用して `Play/Pause`、`Mute/Unmute`、`Enable/Disable CCs` のボタンコントロール要素を組み込みます。

また、地域の規制によっては、必須の通知に**コールアウト**または**ポップアップ**が必要になる場合がありますのでご注意ください。

実際には、インターフェイスビルダーでプレーヤーコンテナー用に `playerView` という `UIView` を追加します。 このビューは広告セッションにも関連付けられ、OM SDK とやり取りします。

ビデオプレーヤーはプログラムで作成し、このコンテナに追加できます。

```swift
func createMediaPlayer(withAsset asset: AVAsset) {
        let player = AVQueuePlayer()
    let looper = AVPlayerLooper(player: player, templateItem: AVPlayerItem(asset: asset!))

    player.isMuted = true
    self.attachUnMuteButtonImage()

    guard let videoView = playerView else {
      fatalError("VideoView is not initialized")
    }

    let videoPlayerLayer = AVPlayerLayer(player: player)
    videoPlayerLayer.bounds = videoView.bounds
    videoPlayerLayer.frame = videoView.bounds
    videoPlayerLayer.videoGravity = AVLayerVideoGravity.resizeAspect;
    videoPlayerLayer.player?.volume = 1.0;

    videoView.layer.insertSublayer(videoPlayerLayer, at: 0)
}
```

シンプルな `AVPlayer` の代わりに `AVQueuePlayer` と `AVPlayerLooper` を使用して、動画広告が無限にループするようにします。 また、`isMuted` プロパティを true に設定して、動画をデフォルトでミュートした状態で開始します。

メディアアセットは、`let asset = AVAsset(url: localVideoURL)` で作成できます。 クローズドキャプション付きのメディアアセットを作成する方法については、以下のセクションを参照してください。

***

## ステップ3：OMID 広告セッションの設定

クライアントは、プレーヤーが追加のスクリプトを実行できるように OMID をサポートし、Criteo が動画のビューアビリティを追跡できるようにする必要があります。 これにより、Criteo は VAST XML 内で独自の検証スクリプトを送信し、動画のビューアビリティを追跡できるようになります。

<Info>
  **OMID について**

  [Open Measurement Interface Definition（OMID）](https://iabtechlab.com/standards/open-measurement-sdk/) は、さまざまなプラットフォームやデバイスにわたるデジタル広告、特に動画広告のビューアビリティを測定および検証するために IAB が提供する標準化されたテクノロジーです。

  OMID は、業界標準に準拠した、視認可能なインプレッションや視認可能な動画再生などのビューアビリティ指標を測定するための一貫したフレームワークを提供します。 OMID を実装すると、広告のパフォーマンスとユーザー・エンゲージメントに関する正確な分析情報が得られるほか、すべての小売業者にわたって同じ方法で動画のビューアビリティを追跡できるため、ネットワーク全体の一貫性が確保されます。

  このドキュメントの OM SDK は、\*\*モバイルプラットフォーム（iOS）向けの OM SDK を指します。
</Info>

[VAST 仕様](https://www.iab.com/wp-content/uploads/2016/04/VAST4.0_Updated_April_2016.pdf)のセクション3.16では、これらの検証が存在するオプションの `AdVerifications` 要素について概説しています。 `AdVerifications` 内にネストされた各検証要素には、検証に必要なリソースの詳細が記載されています。

<Info>
  **Open-Measurement SDK**

  これらのリソースを処理するため、IAB が提供する Open-Measurement SDK にリソースを渡します。 iOS 向けの公式 OM SDK 実装ガイドについては、[こちら](https://interactiveadvertisingbureau.github.io/Open-Measurement-SDKiOS/)を参照してください。 ドキュメントには、ビューアビリティの統合に必要な、各ステップのコードスニペットが記載されています。
</Info>

大まかに言うと、プロセスは以下のようになります。

* OMID サービススクリプトをロードする
* 広告セッション設定と広告セッションそのものを作成する
* 広告を表示するプレーヤーを登録して広告セッションを開始する
* 動画および広告関連のイベントが発生したときに OM SDK に対して `adEvents` および `mediaEvents` イベントをトリガーする

<Warning>
  [IAB 実装ガイド](https://interactiveadvertisingbureau.github.io/Open-Measurement-SDKiOS/)では、一部の Criteo ビデオプレーヤーの仕様がカバーされていません。 そのため、\*\*[iOS 向けの IAB ドキュメント](https://interactiveadvertisingbureau.github.io/Open-Measurement-SDKiOS/validation.html)\*\*に従い、Criteo 検証スクリプトを使用して統合を検証することを強くお勧めします。
</Warning>

<Frame>
  <img src="/images/retailer-integration/docs/96d3750dc86afc5ab1a34c348239ea59193c70c5c230401225fca1feec67fc7c-image.png" />
</Frame>

<Info>
  Criteo 検証スクリプトは VAST XML ファイルから取得されます。
</Info>

OM SDK を通じて、Criteo OMID 検証スクリプトから Criteo サーバーにいくつかのイベントが送信されます。

<table>
 <thead>
  <tr>
   <th>
    <p>
     レベル
    </p>
   </th>
   <th>
    <p>
     ビーコン名
    </p>
   </th>
   <th>
    <p>
     説明
    </p>
   </th>
  </tr>
 </thead>
 <tbody>
  <tr>
   <td>
    <p>
     OMID（
     <code>
      VerificationParameters
     </code>
     ）
    </p>
   </td>
   <td>
    <p>
     <code>
      omidTrackView
     </code>
    </p>
   </td>
   <td>
    <p>
     OMID 広告セッションが適切に初期化されたことを示します。
    </p>
   </td>
  </tr>
  <tr>
   <td>
    <p>
     OMID（
     <code>
      VerificationParameters
     </code>
     ）
    </p>
   </td>
   <td>
    <p>
     <code>
      start
     </code>
    </p>
   </td>
   <td>
    <p>
     動画が開始され、ビューポートで動画が100%表示されていることを示します。
    </p>
   </td>
  </tr>
  <tr>
   <td>
    <p>
     OMID（
     <code>
      VerificationParameters
     </code>
     ）
    </p>
   </td>
   <td>
    <p>
     <code>
      firstQuartile
     </code>
    </p>
   </td>
   <td>
    <p>
     動画がちょうど25%のクォータイルに到達し、ビューポートで動画が100%表示されていることを示します。
    </p>
   </td>
  </tr>
  <tr>
   <td>
    <p>
     OMID（
     <code>
      VerificationParameters
     </code>
     ）
    </p>
   </td>
   <td>
    <p>
     <code>
      midpoint
     </code>
    </p>
   </td>
   <td>
    <p>
     動画がちょうど50%のクォータイルに到達し、ビューポートで動画が100%表示されていることを示します。
    </p>
   </td>
  </tr>
  <tr>
   <td>
    <p>
     OMID（
     <code>
      VerificationParameters
     </code>
     ）
    </p>
   </td>
   <td>
    <p>
     <code>
      thirdQuartile
     </code>
    </p>
   </td>
   <td>
    <p>
     動画が75%のクォータイルに到達し、ビューポートで動画が100%表示されていることを示します。
    </p>
   </td>
  </tr>
  <tr>
   <td>
    <p>
     OMID（
     <code>
      VerificationParameters
     </code>
     ）
    </p>
   </td>
   <td>
    <p>
     <code>
      complete
     </code>
    </p>
   </td>
   <td>
    <p>
     動画が完了し、ビューポートで動画が100%表示されていることを示します。
    </p>
   </td>
  </tr>
  <tr>
   <td>
    <p>
     OMID（
     <code>
      VerificationParameters
     </code>
     ）
    </p>
   </td>
   <td>
    <p>
     <code>
      twoSecondsFiftyPercentView
     </code>
    </p>
   </td>
   <td>
    <p>
     動画が2秒間連続して再生され、ビューポートで動画が50%以上表示されていることを示します。 動画をバッファリングしたり一時停止したりすると、2秒のカウンターがリセットされることに注意してください。
    </p>
   </td>
  </tr>
  <tr>
   <td>
    <p>
     VAST XML - OMID
    </p>
   </td>
   <td>
    <p>
     <code>
      &lt;Tracking event="verificationNotExecuted"&gt;
     </code>
    </p>
   </td>
   <td>
    <p>
     検証スクリプトの初期化に失敗したことを示します。
    </p>
   </td>
  </tr>
 </tbody>
</table>

<Info>
  ビューポート表示の100%は、クォータイルなどの再生イベントを追跡するための業界標準です。 ビューポートでの連続2秒間と50%の表示は MRC 標準です。
</Info>

***

### 1. OMID サービススクリプトをロードする

この IAB OM SDK は、クリエイティブでビューアビリティをサポートする上で組み込む必要があるスクリプト（`omweb-v1.js`）とモバイルライブラリで構成されています。

<Note>
  **スクリプトは、自身でホストすることも、コピーをホストしている Criteo の CDN から取得することもできます。 ファイルは、[こちらのページ](https://static.criteo.net/banners/android/1.5.2/omsdk-v1_1.5.2.js)から確認できます。**

  `omweb-v1.js` は、ウェブコンテキスト向けの OMID JS ライブラリです。
</Note>

***

### 2. 広告セッション設定と広告セッションそのものを作成する

広告セッションを初期化する前に、OM SDK をアクティブ化する必要があります。 また、広告セッションでは、Criteo の統合に関連するパラメータを指定する必要があります。

<Info>
  OMID `VerificationScript`、`VerificationParameters`、および `VendorKey` は VAST 内で提供されます。
</Info>

```swift expandable
private static func createAdSession(adView: UIView?, vendorKey:String, verificationScriptURL: String, verificationParameters: String) -> OMIDCriteoAdSession {
// OMID が有効化されていることを確認します
    guard OMIDCriteoSDK.shared.isActive else {
        fatalError("OMID is not active")
    }
// 広告セッションのコンテキストを取得します 以下を参照してください
    let context = createAdSessionContext(withPartner: partner, adView: adView, vendorKey: vendorKey, verificationScriptURL: verificationScriptURL, verificationParameters: verificationParameters)
// 広告セッションの構成を取得します 以下を参照してください
    let configuration = createAdSessionConfiguration()

    do {
// 広告セッションを作成します
        let session = try OMIDCriteoAdSession(configuration: configuration, adSessionContext: context)

        print("Session created")
// 測定用に広告のメインビューを提供します
        guard let adView = adView else {
            fatalError("Ad View is not initialized")
        }
        session.mainAdView = adView
        return session
    } catch {
        fatalError("Unable to instantiate ad session: \(error)")
    }

}
    
private static func createAdSessionContext(withPartner partner: OMIDCriteoPartner, adView: UIView?, vendorKey: String, verificationScriptURL: String, verificationParameters: String) -> OMIDCriteoAdSessionContext {
    do {  
// 広告レスポンスで提供された値を使用して検証リソースを作成します
        guard let verificationResource = createVerificationScriptResource(vendorKey: vendorKey,
                                                                          verificationScriptURL: verificationScriptURL,
                                                                          parameters: verificationParameters)
        else {
            fatalError("Unable to instantiate session context: verification resource cannot be nil")
        }
    
    
        return try OMIDCriteoAdSessionContext(partner: partner,
                                        script: omidJSService,
                                        resources: [verificationResource],
                                        contentUrl: nil,
                                        customReferenceIdentifier: nil)
    
    } catch {
        fatalError("Unable to create ad session context: \(error)")
    }
}

private static func createAdSessionConfiguration() -> OMIDCriteoAdSessionConfiguration {
    do {
        return try OMIDCriteoAdSessionConfiguration(creativeType: .video,
                                                  impressionType: .beginToRender,
                                                  impressionOwner: .nativeOwner,
                                                  mediaEventsOwner: .nativeOwner,
                                                  isolateVerificationScripts: false)
    
    } catch {
        fatalError("Unable to create ad session configuration: \(error)")
    }
}
```

***

### 3. 広告を表示するプレーヤーを登録して広告セッションを開始する

広告セッションが開始されると、Criteo 検証スクリプトが自動的に読み込まれます。 この時点で、Criteo 検証スクリプトは、OM SDK に対してトリガーされるすべての `AdEvents` と `MediaEvents` の監視を開始します。

```swift
func startSession() {
    print("Starting session for \(adSession.debugDescription)")

    createAdEventsPublisher()
    createMediaEventsPublisher()

    adSession.start()
}

private func createAdEventsPublisher() {
// セッション開始前にイベントパブリッシャーを作成します
    do {
        self.adEvents = try OMIDCriteoAdEvents(adSession: adSession)
    } catch {
        fatalError("Unable to instantiate OMIDAdEvents: \(error)")
    }
}

private func createMediaEventsPublisher() {
    do {
        self.mediaEvents = try OMIDCriteoMediaEvents(adSession: adSession)
    } catch {
        fatalError("Unable to instantiate OMIDMediaEvents: \(error)")
    }
}
```

***

### 4. 動画と広告関連のイベントが発生したときに、OM SDK に `adEvents` および `mediaEvents` イベントをトリガーします。

<Note>
  **利用可能な `AdEvents` と `MediaEvents` の完全なリストには [IAB の Github](https://github.com/InteractiveAdvertisingBureau/) からアクセスできます。**
</Note>

これらの呼び出しを通じて、Criteo 検証スクリプトは高度なビューアビリティ・イベントを計算できるようになります。

* Criteo はクォータイルイベントを追跡することができるようになります（動画がビューポートの100%を占めている場合）
* 高度なイベントは、動画の50%が2秒間連続して再生され、ビューポートに表示されているかどうかを追跡するために計算されます。

`AdEvents` と `MediaEvents` の両方に対するすべての呼び出しは、クォータイルおよびディスプレイ・イベントの既存のトラッキングビーコンとは別にトリガーされる必要があります。 また、動画バッファリング・イベントをトリガーする必要があります。

OMID イベントのトリガーは VAST トラッカーのトリガーと似ています。以下のセクションで両方について説明します。

***

## ステップ4：動画再生中にビーコンを起動する

クリエイティブがダウンロードされると、OMID セッションを開始し、`loaded` イベントをトリガーできます。

```swift
func didFinishFetchingCreative() {
  sessionInteractor?.startSession()
// VAST プロパティを OMID にレポートします
  let VASTProperties = OMIDCriteoVASTProperties(autoPlay: true, position: .standalone)
  sessionInteractor?.fireAdLoaded(vastProperties: VASTProperties)
// プレーヤーを準備し、リスナーを紐づけます
  createMediaPlayer(withAsset: getVideoAndCaptions()!)
  addQuartileTrackingToVideoPlayer()
  attachPauseButtonImage()
  presentAd()
}
```

広告が表示されたらすぐに、VAST から OMID に `impression` ビーコンを送信できます。

```swift
func emitBeacon(type: Beacon) {        
  let task = URLSession.shared.dataTask(with: self.tracking![type.rawValue]!);
  task.resume()
}

func didPresentAd() {
   emitBeacon(type: .impression);
   sessionInteractor?.fireImpression()
 }
```

`start`、`firstQuartile`、`midpoint`、`thirdQuartile`、および `complete` ビーコンは、ポーリングスレッドによってトリガーされる関数で管理されます。 再生イベントごとに、VAST ビーコンと OMID ビーコンの両方を送信します。

```swift expandable
func addQuartileTrackingToVideoPlayer() {
  guard let player = player else { return }
  player.addPeriodicTimeObserver(forInterval: CMTimeMakeWithSeconds(1, preferredTimescale: 10),
                                 queue: DispatchQueue.main) { [weak self] _ in
    guard let self = self else { return }
    self.recordQuartileChange()
  }
  currentQuartile = .tracking
}

func recordQuartileChange() {
  guard let quartile = currentQuartile,
  let currentItem = player?.currentItem
  else {
    return
  }

  let currentTime = currentItem.currentTime().seconds
  let duration = currentItem.duration.seconds
  guard duration > 0 else { return }

  let progress = currentTime / duration
  switch quartile {
    case .tracking:
    if (progress > 0) {
      emitBeacon(type: .start);

      sessionInteractor?.getMediaEventsPublisher().start(withDuration: CGFloat(duration),
                                                         mediaPlayerVolume: playerVolume())
      currentQuartile = .start
    }
    case .start:
    if (progress > 0.25) {
      emitBeacon(type: .firstQuartile);

      sessionInteractor?.getMediaEventsPublisher().firstQuartile()
      currentQuartile = .firstQuartile
    }
    case .firstQuartile:
    if (progress > 0.5) {
      emitBeacon(type: .midpoint);

      sessionInteractor?.getMediaEventsPublisher().midpoint()
      currentQuartile = .midpoint
    }
    case .midpoint:
    if (progress > 0.75) {
      emitBeacon(type: .thirdQuartile);

      sessionInteractor?.getMediaEventsPublisher().thirdQuartile()
      currentQuartile = .thirdQuartile
    }
    case .thirdQuartile:
    if (progress >= 1.0) {
      emitBeacon(type: .complete);

      sessionInteractor?.getMediaEventsPublisher().complete()
      currentQuartile = .complete
    }
    case .complete:
    break
  }
}
```

<Warning>
  \*\*[正確なビーコンのトラッキング](/retailer-integration/docs/tracking-ad-related-activity-japanese)**を確実に行うには、**&#x518D;生ビーコ&#x30F3;**（開始ビーコンとクォータイルビーコン)**&#x304C;**動画の最初の再生時かつ動画の再生中に1回だけ発信される必要があります。**

  ただし、**インタラクションビーコン**（クリック、ミュート、ミュート解除、一時停止、再開など）は、**対応するユーザーアクションが発生するたびに発信される必要があります。**
</Warning>

ボタンにイベントリスナーをアタッチして、**一時停止/再開**ビーコンを処理します。 プレーヤーが再生中の場合は一時停止し、OMID および VAST 一時停止ビーコンを発信します。 それ以外の場合は、再開ビーコンを発信します。 また、再生ボタンと一時停止ボタンを表すボタンの画像も変更します。

```swift
@IBAction func tappedPlayingControl() {
  changePlayingControls()
}

func changePlayingControls() {
  if let player = player, player.rate == 0 {
    play()
    attachPauseButtonImage()
    sessionInteractor?.getMediaEventsPublisher().resume()
    emitBeacon(type: .resume)
  } else {
    pause()
    attachPlayButtonImage()
    sessionInteractor?.getMediaEventsPublisher().pause()
    emitBeacon(type: .pause)
  }
}
```

**クリック**ビーコンを処理するには、Interface Builder で `Tap Gesture Recognizer` を作成します。これは `playerView` にリンクされ、コントローラーで `clickHandler` アクションをトリガーします。 この関数では、OMID と VAST の `click` ビーコンを送信し、*リダイレクト URL が提供されている場合のみ*、ブラウザに VAST タグから URL を開くように指示します。

リダイレクト URL が提供されない場合、動画を一時停止/再開し、対応するトラッキングビーコンをトリガーします。

```swift
@IBAction func handleClick() {
  let clickThroughURL = self.tracking![Beacon.click.rawValue];

  if((clickThroughURL) != nil) {
    sessionInteractor?.getMediaEventsPublisher().adUserInteraction(withType: .click)
    emitBeacon(type: .click)

    UIApplication.shared.open(clickThroughURL!)
  } else {
    changePlayingControls();
  }
}
```

最後に、ボタンにリスナーをアタッチして、**ミュート/ミュート解除** ビーコンを同様に処理します。  プレーヤーの状態、ボタンの画像を変更し、VAST および OMID トラッキングビーコンをトリガーします。

```swift
@IBAction func toggleMute() {
  player.isMuted = !player.isMuted
  player.isMuted ? attachUnMuteButtonImage() : attachMuteButtonImage();

  sessionInteractor?.getMediaEventsPublisher().volumeChange(to: playerVolume())
  emitBeacon(type: player.isMuted ? .mute : .unmute);
}
```

<Info>
  **OMID とボタン**

  OM SDK はビデオプレーヤー `UIView` にアクセスできるため、表示されている動画の割合をチェックして送信します。 したがって、ボタンがプレーヤーのオーバーレイ上にある場合は、必ず宣言してください。

  ```swift
   adSession.addFriendlyObstruction(element, // muteButton, playButton
                                    purpose: .mediaControls,
                                    detailedReason: "Media Controls over video");
  ```
</Info>

***

## ステップ5：広告セッションを終了する

プレーヤーと広告セッションの破棄を正しく処理することが重要です。

```swift
func willDismissAd() {
// 測定を完了します
    print("Ending measurement session now")
    sessionInteractor?.stopSession()
}

func didDismissAd() {
// 広告を破壊します
    destroyAd()
}

/**
  Releases any resources that the ad may still be using after dismissal.
  */
func destroyAd() {
    hidePlayerControls()

    guard let videoPlayerLayer = playerLayer else { return }
    videoPlayerLayer.player = nil
    videoPlayerLayer.removeFromSuperlayer()
    try? FileManager.default.removeItem(at: localAssetURL)
}
```

***

## 補遺：クローズドキャプション（CC）の取り扱い

動画 URL のみを使用して `AVAsset` を作成する代わりに、3つのトラックを持つ `AVMutableComposition` を作成します。

* 1つ目は動画用、
* 2つ目はオーディオ用、
* 3つ目はキャプション用です。

コード例全文は以下のとおりです。

```swift coffeescript Swift expandable
func getVideoAndCaptions() -> AVMutableComposition? {
  let videoAndCaptions = AVMutableComposition()
  let videoTrack = videoAndCaptions.addMutableTrack(withMediaType:.video , preferredTrackID: kCMPersistentTrackID_Invalid)

  let localVideoAsset = AVURLAsset(url: localAssetURL)
  do {

    guard localVideoAsset.tracks.count > 0 else
    {
// エラーメッセージ
      return nil;
    }
    try? videoTrack?.insertTimeRange(CMTimeRangeMake(start: CMTime.zero, duration: localVideoAsset.duration),
                                     of: localVideoAsset.tracks(withMediaType: .video)[0],
      at: CMTime.zero)
  }

  let audioTrack = videoAndCaptions.addMutableTrack(withMediaType:.audio , preferredTrackID: kCMPersistentTrackID_Invalid)
  do {

    guard localVideoAsset.tracks.count > 0 else
    {
// エラーメッセージ
      return nil;
    }
    try? audioTrack?.insertTimeRange(CMTimeRangeMake(start: CMTime.zero, duration: localVideoAsset.duration),
                                     of: localVideoAsset.tracks(withMediaType: .audio)[0],
      at: CMTime.zero)
  }

  let subtitleAsset = AVURLAsset(url: localCaptionsURL)
  let subtitleTrack = videoAndCaptions.addMutableTrack(withMediaType: .text, preferredTrackID: kCMPersistentTrackID_Invalid)
  do {
    guard subtitleAsset.tracks.count > 0 else{
// エラーメッセージ
      return nil;
    }
    try? subtitleTrack?.insertTimeRange(CMTimeRangeMake(start: CMTime.zero, duration: localVideoAsset.duration),
                                        of: subtitleAsset.tracks(withMediaType: .text)[0],
      at: CMTime.zero)
  }
  return videoAndCaptions;
}
```

<Info>
  **注記**

  iOS 15 以前では、`AVMutableComposition` はローカルに保存されたアセットの挿入のみをサポートしており、リモートアイテムは使用できません。 そのため、プレーヤーを初期化する前に、メディアファイルとクローズドキャプションをローカルにダウンロードして保存します。
</Info>

```swift coffeescript Swift
public func fetch(_ remoteURL: URL, 
                  _ localAssetURL: URL,
                  _ statusHandler: @escaping (_ message: String) -> (), 
                                _ completionHandler: @escaping (_ fileURL: URL) -> (), 
                                    _ errorHandler: @escaping (_ message: String) -> ()) {
                    
  DispatchQueue.main.async {
    statusHandler("Fetching file...");
  }

  self.creativeDownloadTask = URLSession.shared.downloadTask(with: remoteURL) {
    (fileURL, response, error) in
      guard let fileURL = fileURL else {
        DispatchQueue.main.async {
          errorHandler("Unable to fetch file from remote URL: \(remoteURL)")
        }
        return
      }
    try? FileManager.default.moveItem(at: fileURL, to: localAssetURL)

    DispatchQueue.main.async {
      completionHandler(fileURL)
    }
  }

  self.creativeDownloadTask?.resume()
}
```
