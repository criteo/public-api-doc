---
title: Brands (Private Market) (2024-04)
original_file: to-migrate/criteo-dev-portal-full-export/merged_export/Retail_Media/v2026-preview/docs/Private market (target Only)/brands-private-market-2024-04.md
hidden: true
---

import { EndpointBadge } from "/snippets/EndpointBadge.jsx";

<Note>
  ### A FEW THINGS TO KNOW

  1 - A *brand* is a collection of products marketed & sold under the same umbrella name.\
   2 - Brands associated with the account determine the products available for an account to promote on retailer sites.\
  3 - An account may have access to one or more brands, typically managed by Criteo.\
  4 - Brand attributes are standardized across retailers
</Note>

## **Endpoints**

<table>
 <thead>
  <tr>
   <th>
    <p>
     Verb
    </p>
   </th>
   <th>
    <p>
     Endpoint
    </p>
   </th>
   <th>
    <p>
     Description
    </p>
   </th>
  </tr>
 </thead>
 <tbody>
  <tr>
   <td>
    <p>
     <b>
      GET
     </b>
    </p>
   </td>
   <td>
    <p>
     <code>
      /accounts/&#123;private-market-accountId&#125;/brands
     </code>
    </p>
   </td>
   <td>
    <p>
     Get Account Brands
    </p>
   </td>
  </tr>
  <tr>
   <td>
    <p>
     <b>
      POST
     </b>
    </p>
   </td>
   <td>
    <p>
     <code>
      /account-management/accounts/&#123;accountId&#125;/brands/add
     </code>
    </p>
   </td>
   <td>
    <p>
     Add Brand to a PM Demand-Brand account
    </p>
   </td>
  </tr>
  <tr>
   <td>
    <p>
     <b>
      POST
     </b>
    </p>
   </td>
   <td>
    <p>
     <code>
      /account-management/accounts/&#123;accountId&#125;/brands/remove
     </code>
    </p>
   </td>
   <td>
    <p>
     Remove Brand from a PM Demand-Brand account
    </p>
   </td>
  </tr>
  <tr>
   <td>
    <p>
     <b>
      PUT
     </b>
    </p>
   </td>
   <td>
    <p>
     <code>
      /account-management/accounts/&#123;accountId&#125;/sellers
     </code>
    </p>
   </td>
   <td>
    <p>
     Add Seller to PM Demand-Seller Account
    </p>
   </td>
  </tr>
 </tbody>
</table>

## **Brand Attributes**

<ParamField path="accountId" type="string">
  Account ID

  **Values:** int64
</ParamField>

<ParamField path="brands" type="array">
  Brand IDs

  **Values:** string
</ParamField>

<ParamField path="sellerId" type="string">
  Seller ID

  **Values:** int64
</ParamField>

<ParamField path="retailerId" type="string">
  Retailer ID

  **Values:** int64
</ParamField>

## **Get All Brands**

This endpoint lists all brands associated with an account.  Results are paginated.

<EndpointBadge method="get">
```http
https://api.criteo.com/preview/retail-media/accounts/{private-market-accountId}/brands
```
</EndpointBadge>

**Sample Request** 

*In the example, the API calls for all the brands that are mapped to the private market account ID`433350412678656000`*

<CodeGroup>

```bash cURL
curl -L 'https://api.criteo.com/preview/retail-media/accounts/433350412678656000/brands' \
-H 'Accept: application/json' \
-H 'Authorization: Bearer <MY_ACCESS_TOKEN>'
```

```python Python
import http.client

conn = http.client.HTTPSConnection("api.criteo.com")
payload = ''
headers = {
  'Accept': 'application/json',
  'Authorization': 'Bearer <MY_ACCESS_TOKEN>'
}
conn.request("GET", "/preview/retail-media/accounts/433350412678656000/brands", payload, headers)
res = conn.getresponse()
data = res.read()
print(data.decode("utf-8"))
```

```java Java
OkHttpClient client = new OkHttpClient().newBuilder()
  .build();
MediaType mediaType = MediaType.parse("text/plain");
RequestBody body = RequestBody.create(mediaType, "");
Request request = new Request.Builder()
  .url("https://api.criteo.com/preview/retail-media/accounts/433350412678656000/brands")
  .method("GET", body)
  .addHeader("Accept", "application/json")
  .addHeader("Authorization", "Bearer <MY_ACCESS_TOKEN>")
  .build();
Response response = client.newCall(request).execute();
```

```php PHP
<?php
require_once 'HTTP/Request2.php';
$request = new HTTP_Request2();
$request->setUrl('https://api.criteo.com/preview/retail-media/accounts/433350412678656000/brands');
$request->setMethod(HTTP_Request2::METHOD_GET);
$request->setConfig(array(
  'follow_redirects' => TRUE
));
$request->setHeader(array(
  'Accept' => 'application/json',
  'Authorization' => 'Bearer <MY_ACCESS_TOKEN>'
));
try {
  $response = $request->send();
  if ($response->getStatus() == 200) {
    echo $response->getBody();
  }
  else {
    echo 'Unexpected HTTP status: ' . $response->getStatus() . ' ' .
    $response->getReasonPhrase();
  }
}
catch(HTTP_Request2_Exception $e) {
  echo 'Error: ' . $e->getMessage();
}
```
</CodeGroup>

**Sample Response** 

*The response returns only the brands that are mapped to account ID`433350412678656000`*

```json JSON
{
    "metadata": {
        "totalItemsAcrossAllPages": 2,
        "currentPageSize": 25,
        "currentPageIndex": 0,
        "totalPages": 1
    },
    "data": [
        {
            "id": "2149971499",
            "type": "RetailMediaBrand",
            "attributes": {
                "name": "timberland"
            }
        },
        {
            "id": "2149971500",
            "type": "RetailMediaBrand",
            "attributes": {
                "name": "timberland pro"
            }
        }
    ]
}
```

## **Add Brand to Account**

This endpoint adds a brand to a Private Market Demand-Brand account. 

<EndpointBadge method="post">
```http
https://api.criteo.com/preview/retail-media/account-management/accounts/{accountId}/brands/add
```
</EndpointBadge>

**Sample Request**

<CodeGroup>

```bash Bash
```

```python Python
```

```java Java
```

```php PHP
```
</CodeGroup>

**Sample Response**

```json
{
    "foo": "bar"
}
```

## **Remove Brand from Account**

This endpoint removes a brand from a Private Market Demand-Brand account. 

<EndpointBadge method="post">
```http
https://api.criteo.com/preview/retail-media/account-management/accounts/{accountId}/brands/remove
```
</EndpointBadge>

**Sample Request**

<CodeGroup>

```bash Bash
```

```python Python
```

```java Java
```

```php PHP
```
</CodeGroup>

**Sample Response**

```json
{
    "foo": "bar"
}
```

## **Add Seller to Account**

This endpoint adds a Seller to a Private Market Demand-Seller account.

<EndpointBadge method="put">
```http
https://api.criteo.com/preview/retail-media/account-management/accounts/{accountId}/sellers
```
</EndpointBadge>

**Sample Request**

<CodeGroup>

```bash Bash
```

```python Python
```

```java Java
```

```php PHP
```
</CodeGroup>

**Sample Response**

```json
{
    "foo": "bar"
}
```

## **Responses**

<table>
 <thead>
  <tr>
   <th>
    <p>
     Response
    </p>
   </th>
   <th>
    <p>
     Descriptoin
    </p>
   </th>
  </tr>
 </thead>
 <tbody>
  <tr>
   <td>
    <p>
     🔵
    </p>
    <p>
     <code>
      200
     </code>
    </p>
   </td>
   <td>
    <p>
     Call executed with success
    </p>
   </td>
  </tr>
  <tr>
   <td>
    <p>
     🔴
    </p>
    <p>
     <code>
      403
     </code>
    </p>
   </td>
   <td>
    <p>
     API user does not have the authorization to make requests to the account ID. For an authorization request, follow the
    </p>
    <p>
     <a href="/retail-media/v2026-preview/docs/authorization-requests">
      authorization request
     </a>
    </p>
    <p>
     steps
    </p>
   </td>
  </tr>
 </tbody>
</table>
