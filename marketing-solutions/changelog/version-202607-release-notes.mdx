---
title: Version 2026.07 Release Notes
---

<Info>
  Version **`2026.07`** of the Criteo Marketing Solutions API is live as of **July 21st, 2026** and will be supported until **[date TBD]**. A new Postman collection for `2026.07` is also available in the [Criteo Postman workspace](https://www.postman.com/realcriteo/workspace/criteo/overview).
</Info>

***

This version introduces new capabilities to the Criteo Marketing Solutions API and changes to existing endpoints. **Only endpoints with changes are listed here.**

With the introduction of this new version, the base URL changes to:

```text URL
https://api.criteo.com/2026-07/{endpoint}
```

***

# New in Stable 2026.07

## Product Boost

Five new endpoints are available to manage Product Boost configurations at the ad and dataset level.

| Verb | Endpoint | Description |
|---|---|---|
| **GET** | `/marketing-solutions/ads/{ad-id}/product-boost` | List Product Boost configurations for an ad |
| **GET** | `/marketing-solutions/ads/{ad-id}/product-boost/{product-set-id}` | Get a specific Product Boost configuration |
| **POST** | `/marketing-solutions/ads/{ad-id}/product-boost/{product-set-id}` | Create a Product Boost configuration |
| **DELETE** | `/marketing-solutions/ads/{ad-id}/product-boost/{product-set-id}` | Delete a Product Boost configuration |
| **GET** | `/marketing-solutions/dataset/{dataset-id}/product-boost` | List Product Boost configurations for a dataset |

***

## Ad-sets — New Fields

The following field has been added to ad-set endpoints (`PATCH`, `POST`, search, and `GET`):

- `budget.pacingBehavior` (string, nullable) — controls how the budget is paced over the campaign flight.

<Info>
  See the full documentation for [Ad-set endpoints](/marketing-solutions/docs/ad-set).
</Info>

***

## Ad-sets — New Attribution Method Values

Two new values have been added to the `attributionConfiguration.attributionMethod` enum, available on all ad-set endpoints (`PATCH`, `POST`, search, and `GET`):

- `googleAnalytics`
- `sftp`

<Info>
  See the full documentation for [Ad-set endpoints](/marketing-solutions/docs/ad-set).
</Info>

***

# Moving from Preview to Stable

No features were promoted from preview to stable in this release.

***

# New in Experimental

The Experimental version of the Marketing Solutions API reaches full endpoint parity with the previous Preview version in this release. No net-new endpoints or fields have been introduced.
